/**
 * @deprecated El aviso «asume la clínica» quedó unificado en EstadoBadge /
 * labelEstadoDietaVisible. Se conserva por compatibilidad; no renderiza nada.
 */
export function SalidaClinicaSostenidaBadge(_props: {
  activo?: boolean
  className?: string
}) {
  return null
}
